version: 2

project_name: agentshield

before:
  hooks:
    - go mod tidy

builds:
  - id: agentshield
    main: ./cmd/agentshield
    binary: agentshield
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X 'github.com/AI-AgentLens/agentshield/internal/cli.Version={{.Version}}'
      - -X 'github.com/AI-AgentLens/agentshield/internal/cli.GitCommit={{.ShortCommit}}'
      - -X 'github.com/AI-AgentLens/agentshield/internal/cli.BuildDate={{.Date}}'

archives:
  - id: default
    formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      # Community shell + MCP packs are embedded in the binary via //go:embed
      # in packs/packs.go, so we no longer ship them as separate YAML files.
      # The old `packs/*.yaml` glob matched nothing anyway (real shell packs
      # live at packs/community/*.yaml) and silently left brew cask users
      # without protection. Keep this file list minimal.
      - scripts/agentshield-wrapper.sh
      - configs/default_policy.yaml
      - src: compliance-bin/agentcompliance_{{ .Os }}_{{ .Arch }}/agentcompliance
        dst: .
        strip_parent: true

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

# macOS .app bundles and DMGs are built post-GoReleaser by CI:
#   VERSION={{.Version}} ./scripts/build-app.sh arm64
#   VERSION={{.Version}} ./scripts/build-app.sh amd64
#   VERSION={{.Version}} ./scripts/build-dmg.sh arm64
#   VERSION={{.Version}} ./scripts/build-dmg.sh amd64
# DMGs are uploaded alongside tar.gz archives to /releases/v{{.Version}}/
#
# Cask is generated by CI and pushed to homebrew-tap with aiagentlens.com download URLs
# (not using GoReleaser's homebrew_casks since the repo is private)
